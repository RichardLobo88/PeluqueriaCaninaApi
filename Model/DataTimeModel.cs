using System.Globalization;
using System.Runtime.Serialization;
/*
namespace PeluqueriaCaninaApi.Model
{
    public class DataTimeModel
    {
        DateTime date = new DateTime();

        string formato = DateTime.Now.ToString("dd/M/yyyy", CultureInfo.InvariantCulture);


    }

   public DateTime SumarTurno(DateTime date) => date.AddMinutes(30);


    public DateTime RestarTurno(DateTime date) => date.AddMinutes(-30);

}
*/